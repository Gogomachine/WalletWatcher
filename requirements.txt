# Requires Python 3.11 or 3.12
# Python 3.14 is not yet fully supported by all dependencies

# Core bot dependencies
aiogram>=3.7.0
aiohttp>=3.9.5
solana>=0.30.0
web3>=6.15.0
python-dotenv>=1.0.0

# Database - SQLite (legacy, for migration)
aiosqlite>=0.19.0

# Database - PostgreSQL (production)
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.23

# Caching and state storage
redis[asyncio]>=5.0.1

# Screenshots
playwright>=1.40.0
